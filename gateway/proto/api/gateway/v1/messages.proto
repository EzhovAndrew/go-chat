syntax = "proto3";

package api.gateway.v1;

option go_package = "github.com/go-chat/gateway/pkg/api/v1;gatewayv1";

// Gateway doesn't have its own service definitions
// It acts as a reverse proxy for other services
// This file is a placeholder for any gateway-specific messages if needed in the future

