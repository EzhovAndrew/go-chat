syntax = "proto3";

package api.gateway.v1;

option go_package = "github.com/go-chat/gateway/pkg/api/v1;gatewayv1";

// Gateway doesn't define its own gRPC service
// It translates REST requests to gRPC calls to backend services
// This file is a placeholder for any gateway-specific services if needed in the future

